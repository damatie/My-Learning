export const initialState  =  {
    user:'hello',
    fromEditBox:true,
    fromReplyBox:false,
    replyForwardContainer:false,
}

const reducer = (state,action) => {
    switch(action.type){
            default:
                return state;
        }
    }

export default reducer